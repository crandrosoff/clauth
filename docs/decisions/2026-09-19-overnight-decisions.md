# Overnight decisions — codex parity program (2026-09-19)

Owner asleep 01:46 → 09:00 MDT (supervision revision 188, sleeping until 2026-09-19T15:00Z).
Decisions made without asking, each with its undo. Read this first in the morning.

| # | Decision | Why | Decided against | Undo |
|---|---|---|---|---|
| D1 | Consult egress granted to `gpt` at the sleeping declaration. | Owner's own words: "get astra to review the plan and issue before added upstream" — Astra is the Codex/gpt route. | Asking again (owner already asleep). | `~/.rawgentic/bin/rg-hook supervision_admin.py declare … --clear-consult-grant`. Note: the write dropped an older `glm` grant; re-grant with `--provider glm --granted` if wanted. |
| D2 | No departure preflight for `epic-475` / `epic-1464` driver-state files. | Neither is driven by this session; both predate it and carry no campaign id. | Sweeping them. | None needed. |
| D3 | Spec lives in the fork as `docs/codex-parity-plan.md` (upstream's spec style, next to `docs/codex-plan.md`), on branch `docs/codex-parity-plan`. HTML render committed beside it. | Contributable as-is; upstream works from spec + rulings. | `docs/superpowers/specs/…` (superpowers default). | `git branch -D docs/codex-parity-plan` in the fork. |
| D4 | Upstream issue text drafted and HELD. Slice A branch pushed to the fork, PR HELD. | Owner: hold the issue for the morning read; a PR before the plan issue would be out of order. | Filing overnight. | Nothing to undo; files sit in the fork. |
| D5 | Slice A installed into the running binary only if: red→green tests, whole suite matches baseline, cross-model review (gpt-6-astra, high) clean. `follow_active` defaults OFF. | Owner authorized install-when-passing. | Installing unreviewed. | `ln -sfn ~/.cargo/bin/clauth.0.15.2-backup ~/.local/bin/clauth`, or `cargo install --git https://github.com/uwuclxdy/clauth --branch mommy --force`. |
| D6 | Trusted `/home/rocky00717/rawgentic/projects/clauth` in codex (`~/.codex/config.toml` `[projects."…/clauth"] trust_level = "trusted"`). | The runner's Astra launch timed out because codex asks the trust question before its banner; answering it once unblocks every later launch in this checkout. Persistent and host-wide. | Launching Astra elsewhere (then it could not read the spec in place). | Delete that `[projects."/home/rocky00717/rawgentic/projects/clauth"]` block from `~/.codex/config.toml`. |
| D7 | Astra review launched only after 08:11 UTC, when profile `work`'s weekly window resets. | codex printed "less than 5% of your weekly limit left" on `work`; a high-effort review could exhaust it. `personal` is at 98% and resets later (12:30 UTC). | Running now on `work`; switching to `personal`; a Claude reviewer (owner pinned Astra). | None needed; the wait costs about 12 minutes. |
| D8 | Handed the run to a successor pane at ~59% context, after the spec, its page and the slice-A plan were committed and pushed. | The context meter's directive; a fresh window builds slice A better than a crowded one. | Building A in this window and risking a mid-task compaction. | None; the successor reads `claude_docs/handoffs/2026-09-19-clauth-codex-parity-resume.md`. |
| D9 | Registered `clauth` as a rawgentic workspace project (`.rawgentic_workspace.json` entry + `projects/clauth/.rawgentic.json`, untracked in the fork). | A successor session binds to a project, and the session fence only lets it write inside the bound project's path; without an entry it could not touch the clauth clone. | Binding the successor to another project (fenced out of clauth). | Remove the `clauth` entry from `.rawgentic_workspace.json` (backup: `.rawgentic_workspace.json.bak-before-clauth-20260919`) and delete `projects/clauth/.rawgentic.json`. |
| D10 | Astra's spec review runs through `codex exec` (headless, `-s read-only`, gpt-6-astra, effort high) instead of the herdr pane. | The runner's pane stalled twice: a `/status` probe left `/status<…>` in the composer, and after clearing it a resent paste never submitted on Enter. `codex exec` takes the prompt on stdin and prints the answer; same model, same effort, same read-only sandbox. | Keep fighting the pane; a Claude reviewer (owner pinned Astra). | None needed; the stalled pane is closed, scrollback at `/tmp/astra-pane-w0pE-scrollback.txt`. |
| D11 | **Slice A is NOT built overnight.** Astra (gpt-6-astra, high, read-only) returned `VERDICT: DISAGREE` with a source-backed falsifying case: codex 0.155's auth manager (`reload_if_account_id_matches`) refuses a reloaded `auth.json` whose account differs from the cached one, so a running codex never adopts the new account — it errors. Worse, codex persists refreshed tokens by PATH: a repoint during an in-flight refresh can write account A's new tokens into B's store and leave A's spent token replayable. Full text: `docs/superpowers/astra-spec-review-verdict.md`. | The owner's rule: Astra disagrees → the owner decides; the owner is asleep, so the reversible choice is to build nothing on a design a reviewer just falsified. | Building A as specified; a Claude second opinion. | None needed — nothing was built. The plan file stays as a record of the withdrawn design. |
| D12 | The successor's overnight job changes to spec work only: fold Astra's nine objections into the spec, and draft "A2": launch-time account selection through a `codex` shim (clauth already ships a shim directory on PATH, `~/.local/share/clauth/current@claude/bin`, for claude), so typing `codex` starts the chain's active profile in its own home with no link mutation. No code, no install, no PR, no issue tonight. | It answers the owner's "like Claude" ask without the unsafe link repoint, and it can be judged in the morning. | Idle until morning. | Discard the A2 section if the owner prefers another route. |
| D13 | **The handoff's shim premise was false, and A2 was designed around the correction.** The handoff said clauth already ships a PATH shim directory for claude at `~/.local/share/clauth/current@claude/bin`. It does not. That path is agentgear's PLUGIN materialization tree (`src/plugin_host.rs:442-447`); on this host it holds `.claude-plugin/` and `hooks/`, has no `bin/`, and is on no PATH (verified 2026-09-19). A2 therefore specifies a NEW directory, `<data_dir>/clauth/bin/codex`, with PATH left to the operator and no shell rc file written by clauth. | Writing a spec on a premise that does not hold would have been falsified by the first reader who ran `ls`. | Keeping the handoff's wording. | Delete A2's step 2 and rewrite the shim location; the correction is marked in place with `<!-- correction-shim-dir -->`. |
| D14 | **A2's three hazard answers were decided without the owner:** (a) recursion — clauth resolves `codex` from PATH (`src/runtime.rs:3575-3586`) and would find its own shim, so the spec sets `CLAUTH_CODEX_SHIM_BYPASS=1` on every codex spawn AND bakes absolute paths into the shim, either alone being sufficient; (b) no active codex profile — the shim execs the real codex unchanged rather than failing; (c) Windows — refuse to install, because `resolve_cli_command` prefers a native `.exe` over a `.cmd`, so a `.cmd` shim would be found by a shell and skipped by clauth. | Each is a fork the owner would otherwise have to arbitrate from a blank page; the spec names all three as rulings R3, R3b, R3c so upstream can overturn any of them. | Leaving them open in the spec (it would not then be reviewable). | Change the R3/R3b/R3c answers in `docs/codex-parity-plan.md`; nothing is built, so nothing else moves. |
| D15 | **Slice B offers to split rename and disable/enable into their own PR** (new ruling R4b), rather than insisting they ship inside B. | Astra objection 4: neither writer exists for codex, and `disable` carries refusals (`src/actions.rs:1638-1651`) that must be replicated. Offering the split makes B mergeable even if upstream declines the amendment. | Insisting on the full action set. | Delete R4b and the fallback sentence from slice B. |
| D16 | **The fold gets a repair-verification review, not a new discovery review** — one `codex exec` pass at read-only asking only whether each of Astra's own items is now answered. | The `regression-resistant-review` rule: re-running a broad discovery review after a fix spends the loop-back budget on method error, because discovery finds new things every pass by construction. Consult egress to `gpt` is already granted (D1) and the `work` weekly window reset at 08:11 UTC (D7). | No second review at all (the fold is unreviewed text on a design Astra just falsified). A full re-review. | Ignore the verification report; it is written to `docs/superpowers/astra-fold-verification.md` and changes no other file unless the owner says so. |
| D17 | **The spec's `live` URL is written as the doc-harness CONVENTION url and labelled UNVERIFIED**, not as a confirmed one: `https://2026-09-19-clauth-codex-parity-plan.3dstories.ca`. | I could not verify it from this session. The harness sits behind Cloudflare Access, and a deliberately made-up subdomain (`totally-made-up-name-9f3k2.3dstories.ca`) returns the SAME 200 Access login page, so a 200 proves nothing. The workspace rule says to state the live URL in the companion markdown; stating it as confirmed would have been a false claim. | Writing the URL with no caveat, or omitting it and breaking the standing rule. | Delete the `live |` line from the `provenance` block in `docs/codex-parity-plan.md`, or replace the caveat once the owner opens the URL through Access and confirms it serves. |
